
export const roleConstant = Object.freeze({
    seller: "seller",
    admin: "admin",
    customer:"customer"
})

export const errorMessage = Object.freeze({
  invalidCredentialMessage: "Invalid crediential",
});

